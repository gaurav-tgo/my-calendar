# Gaurav's Curated Calendar

This repository publishes a small iCalendar feed containing only the dates selected in `config.json`.

The calendar is regenerated monthly from the Indian calendar published by Drik Panchang, keeping the current year and the following year available.

To add or remove an event, edit `config.json` and commit the change. The next scheduled run will regenerate `calendar.ics`.
